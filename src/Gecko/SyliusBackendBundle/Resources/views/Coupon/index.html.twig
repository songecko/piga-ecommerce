{% extends 'SyliusBackendBundle::layout.html.twig' %}

{% set promotionId = app.request.get('promotionId') %}

{% import 'SyliusBackendBundle:Macros:buttons.html.twig' as buttons %}
{% from 'SyliusBackendBundle:Macros:misc.html.twig' import pagination %}
{% from 'SyliusBackendBundle:Coupon:macros.html.twig' import list %}

{% block content %}
<div class="page-header">
    <h1>{{ 'sylius.promotion_coupon.index_header'|trans|raw }}</h1>
</div>

<div class="well">
    {{ buttons.show(path('sylius_backend_promotion_show', {'id': promotionId}), 'sylius.show'|trans) }}
    {{ buttons.create(path('sylius_backend_promotion_coupon_create', {'promotionId': promotionId}), 'sylius.promotion_coupon.create'|trans) }}
    {{ buttons.manage(path('sylius_backend_promotion_coupon_generate', {'promotionId': promotionId}), 'sylius.promotion_coupon.generate'|trans) }}
</div>

{{ pagination(promotion_coupons) }}
{{ list(promotion_coupons) }}
{{ pagination(promotion_coupons) }}

{% endblock %}
